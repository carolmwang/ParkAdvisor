<%- include('./partials/header') %>

<form action="/parks" method="GET">
  <label for="name">
  Choose a state!
  <select type="custom-selects" style="width:200px" name="id">
    <% states.forEach((state) => { %>
      <label for="<%= state.id %>">
      <option id="<%= state.id %>" value="<%= state.id %>" name="<%= state.id %>"><%= state.name %></option>
      </label>
    <% }) %>
  </select>
  <input type="submit" value="submit">
  </label>
</form>

<%- include('./partials/foot') %>