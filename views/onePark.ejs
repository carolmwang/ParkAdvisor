<%- include('./partials/header') %>

<h3><%= park.name %></h3>
<div>
  <p><%= park.name %> in <%= park.location %></p>
  <form action= "/parks/<%= park.id %>" method="POST">
    <label for="comment"></label>
    <textarea cols="30" rows= "5" placeholder="Share your experience with <%= park.name %>" name = "comment" id="comment"></textarea>
    <input type="submit" value="submit">
  </form>
  </div>
</div>

<%- include('./partials/foot') %>